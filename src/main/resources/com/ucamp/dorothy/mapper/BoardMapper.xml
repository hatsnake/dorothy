<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ucamp.dorothy.mapper.BoardMapper">

	<select id="list" resultType="com.ucamp.dorothy.domain.Board">
		<![CDATA[
		SELECT board_no as boardNo,
			title,
			content,
			writer,
			reg_date as regDate
		FROM board
		WHERE board_no > 0
		ORDER BY board_no DESC, reg_date DESC
		]]>
	</select>

</mapper>